{% extends 'back.html.twig' %}
{% block body %}

    <div class="col-lg-12 grid-margin stretch-card">
        <div class="card">
            <div class="card-body">
                <button class="btn btn-danger btn-rounded btn-fw btn-icon-text"><a href="{{ path('newActivite') }}">Ajouter une nouvelle Activité</a></button>
                <h4 class="card-title">Toutes les Activitées</h4>
                <div class="table-responsive">
                    <table class="table table-striped">
                        <thead>
                        <tr>

                            <th> Nom</th>
                            <th> Actions </th>

                        </tr>
                        </thead>
                        <tbody>
                        {% for dest in activite %}
                            <tr>
                                <td>{{dest.nomActivite}}</td>
                                <td><a href="{{ path('deleteAct',{'id':dest.id}) }}">supprimer</a>
                                    <a href="{{ path('updateAct',{'id':dest.id}) }}">update</a></td>
                            </tr>
                            </tr>
                        {% endfor %}

                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
{% endblock %}