{% extends 'back.html.twig' %}
{% block body %}
    <div class="row">

        <div class="col-md-6 grid-margin stretch-card">
            <div class="card">
                <div class="card-body">
                    <h4 class="card-title">Modifier une Offre</h4>
                    <p class="card-description"> Modifier</p>
                    {{ form(form) }}
                </div>
            </div>
        </div>
    </div>

{% endblock %}