{% extends 'back.html.twig' %}


{% block body %}
    <h1>User</h1>

    <table class="table">
        <tbody>
        <tr>
            <th>Id</th>
            <td>{{ user.id }}</td>
        </tr>
        <tr>
            <th>Nom</th>
            <td>{{ user.Nom }}</td>
        </tr>
        <tr>
            <th>Prenom</th>
            <td>{{ user.Prenom }}</td>
        </tr>
        <tr>
            <th>Email</th>
            <td>{{ user.Email }}</td>
        </tr>
        <tr>
            <th>Password</th>
            <td>{{ user.Password }}</td>
        </tr>
        <tr>
            <th>Numero</th>
            <td>{{ user.numero }}</td>
        </tr>
        <tr>
            <th>Role</th>
            <td>{{ user.role ? 'Yes' : 'No' }}</td>
        </tr>
        </tbody>
    </table>

    <a href="{{ path('user_index') }}">back to list</a>

    {{ include('user/_delete_form1.html.twig') }}
{% endblock %}
